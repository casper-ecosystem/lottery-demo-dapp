export interface Play {
	deployHash: string;
	isJackpot: boolean;
	playId: string;
	playerAccountHash: string;
	playerPublicKey: string;
	prizeAmount: string;
	jackpotAmount: string;
	roundId: string;
	timestamp: string;
}
